#include <stdio.h>

/**
 * main - Print lowercase in revers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char m;

	for (m = 'z'; m >= 'a'; m--)

	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
